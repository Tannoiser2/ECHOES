{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://echoes.local/schema/chronicle.schema.json",
  "title": "Chronicle definition collection",
  "x-echoes-def": "chronicle",
  "x-echoes-kind": "collection",
  "type": "object",
  "required": [
    "schema_id",
    "version",
    "items"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_id": {
      "const": "chronicle"
    },
    "version": {
      "type": "string"
    },
    "items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/chronicle"
      }
    }
  },
  "$defs": {
    "chronicle": {
      "type": "object",
      "required": [
        "id",
        "title",
        "world_id",
        "start_year",
        "acts",
        "rounds_per_act",
        "action_opportunities_per_round",
        "hand_limit",
        "presence_tokens",
        "entities",
        "regions",
        "act_echo_pools",
        "confluence_templates",
        "opening_text"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^CHR_[A-Z0-9_]+$"
        },
        "title": {
          "type": "string",
          "minLength": 1
        },
        "world_id": {
          "type": "string",
          "minLength": 1
        },
        "start_year": {
          "type": "integer"
        },
        "acts": {
          "type": "integer",
          "minimum": 1
        },
        "rounds_per_act": {
          "type": "integer",
          "minimum": 1
        },
        "action_opportunities_per_round": {
          "type": "integer",
          "minimum": 1
        },
        "hand_limit": {
          "type": "integer",
          "minimum": 1
        },
        "presence_tokens": {
          "type": "integer",
          "minimum": 1
        },
        "max_commit_assets": {
          "type": "integer",
          "minimum": 1,
          "default": 3
        },
        "max_condition_commit_assets": {
          "type": "integer",
          "minimum": 1,
          "default": 2
        },
        "condition_qualified_threshold": {
          "type": "integer",
          "minimum": 1,
          "default": 2
        },
        "minimum_confluences": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "How many Councils this Chronicle guarantees. The world's drift spreads across every question in play and can be one chip short of all of them at once, so a table with nothing to play for produces a year in which nothing was decided. When the last round arrives with fewer Councils than this, the question that came closest is brought to a head. 0 turns the floor off."
        },
        "entities": {
          "type": "array",
          "minItems": 2,
          "items": {
            "type": "string",
            "pattern": "^ENT_[A-Z0-9_]+$"
          }
        },
        "regions": {
          "type": "array",
          "minItems": 2,
          "items": {
            "type": "string",
            "pattern": "^REG_[A-Z0-9_]+$"
          }
        },
        "tensions": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^TEN_[A-Z0-9_]+$"
          },
          "description": "The Tensions this Chronicle runs, written out. Authored form. Use tension_pool instead to assemble a Chronicle from the library, which is what makes Chronicle N+1 possible without writing it (D-028)."
        },
        "drift_distribution": {
          "type": "array",
          "minItems": 1,
          "description": "Drift bag. Omit it when using tension_pool: the bag is then dealt evenly across whatever was drawn.",
          "items": {
            "type": "object",
            "required": [
              "tension_id",
              "count"
            ],
            "additionalProperties": false,
            "properties": {
              "tension_id": {
                "type": "string",
                "pattern": "^TEN_[A-Z0-9_]+$"
              },
              "count": {
                "type": "integer",
                "minimum": 1
              }
            }
          }
        },
        "act_echo_pools": {
          "type": "array",
          "minItems": 1,
          "description": "Dramatic families the Act-end Echo card is drawn from, indexed by act (1-based order). The list is a weighted bag, not a set: repeating a family makes it more likely, and the seeded RNG picks the order in which families are tried. An Act 3 written as [RESOLUTION, RESOLUTION, TURN, RUPTURE] resolves about half the time and can still end mid-crisis (D-030).",
          "items": {
            "type": "object",
            "required": [
              "act",
              "families"
            ],
            "additionalProperties": false,
            "properties": {
              "act": {
                "type": "integer",
                "minimum": 1
              },
              "families": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "type": "string",
                  "enum": [
                    "PRESSURE",
                    "RUPTURE",
                    "TURN",
                    "RESOLUTION",
                    "MEMORIA"
                  ]
                },
                "description": "Repeats are weights, not duplicates to be cleaned up. MEMORIA sta solo nei pool delle Chronicle-biblioteca: e la famiglia delle carte che leggono una leggenda (D-076)."
              }
            }
          }
        },
        "confluence_templates": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^CNF_[A-Z0-9_]+$"
          }
        },
        "starting_relations": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "a",
              "b",
              "level"
            ],
            "additionalProperties": false,
            "properties": {
              "a": {
                "type": "string"
              },
              "b": {
                "type": "string"
              },
              "level": {
                "type": "string",
                "enum": [
                  "ENEMY",
                  "HOSTILE",
                  "NEUTRAL",
                  "ALLY",
                  "BOUND"
                ]
              },
              "tags": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          }
        },
        "starting_control": {
          "type": "array",
          "description": "Chi tiene quale Regione all'apertura. La mappa e' condivisa fra le saghe - sono le stesse sei terre a distanza di secoli - quindi il controllo iniziale non puo' stare nella definizione della Regione: quella porterebbe nella seconda saga le casate della prima. Una Regione non nominata qui parte da quello che dice la Regione.",
          "items": {
            "type": "object",
            "required": [
              "region_id"
            ],
            "additionalProperties": false,
            "properties": {
              "region_id": {
                "type": "string",
                "pattern": "^REG_[A-Z0-9_]+$"
              },
              "entity_id": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "null vuol dire che non la tiene nessuno."
              }
            }
          }
        },
        "global_tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "opening_text": {
          "type": "string",
          "minLength": 1
        },
        "influence_rules": {
          "type": "object",
          "description": "Limits on the INFLUENCE template (§10). Omit the whole object, or any field, to get the original v0.2 behaviour: the presence route works in both directions and there is no per-round cap. See DECISIONS D-021.",
          "additionalProperties": false,
          "properties": {
            "presence_directions": {
              "type": "array",
              "description": "Which directions the free presence route covers. Anything not listed still works, but has to be paid for with a relevant Asset.",
              "items": {
                "type": "string",
                "enum": [
                  "UP",
                  "DOWN"
                ]
              },
              "default": [
                "UP",
                "DOWN"
              ]
            },
            "max_per_entity_per_round": {
              "type": "integer",
              "minimum": 1,
              "description": "How many INFLUENCE actions one Entity may take per round, across all Tensions. Omit for no limit."
            },
            "max_per_tension_per_round": {
              "type": "integer",
              "minimum": 1,
              "description": "How many INFLUENCE actions may touch one Tension per round, by anyone. Bounds how fast the table can move a question against the Drift. Omit for no limit."
            },
            "displacement_on_decrease": {
              "type": "integer",
              "minimum": 0,
              "default": 0,
              "description": "Pushing a Tension down with INFLUENCE raises one of its linked_tensions by this much. Pressure is displaced, not removed: you do not solve a question, you choose which one to have instead. 0 or absent turns it off (D-029)."
            }
          }
        },
        "confluence_rules": {
          "type": "object",
          "description": "Quanto costa stare sul fronte contrario (§12.3). Omesso, vale la regola scritta: su un Failure ogni oppositore si riprende una carta a scelta (D-013).",
          "additionalProperties": false,
          "properties": {
            "opposer_recovers_on_failure": {
              "type": "boolean",
              "default": true,
              "description": "Se false, far cadere una proposta costa quanto proporla: chi si oppone scarta tutto quello che ha impegnato, come il proponente. Prima leva provata contro l'Oppose come strategia dominante (ISSUES 1)."
            },
            "failure_delta": {
              "type": "integer",
              "default": -2,
              "maximum": 0,
              "description": "Di quanto un Failure sfoga la Tensione (appendice A6: -2). A -1 la proposta bocciata non compra quiete: la domanda resta piu' vicina alla soglia e torna prima - la seconda leva contro l'Oppose come strategia dominante, misurata in D-098."
            },
            "focus_weight": {
    "type": "object",
    "additionalProperties": false,
    "description": "Quanto pesa, al Consiglio, stare nella Regione di cui si discute (D-154). Omesso, non pesa niente e vale il comportamento fino a 0.1.118: il titolo conta solo a fine anno. Il peso si somma al fronte di chi lo porta, e solo se quel seggio ha impegnato almeno una carta - un bonus dal nulla sarebbe un voto gratis.",
    "properties": {
     "control": {
      "type": "integer",
      "minimum": 0,
      "maximum": 3,
      "description": "Quanto vale essere il padrone della Regione a fuoco: il titolo che pesa dentro l'anno e non solo nel Destino."
     },
     "majority": {
      "type": "integer",
      "minimum": 0,
      "maximum": 3,
      "description": "Quanto vale avere strettamente piu' presenza di chiunque altro nella Regione a fuoco. A parita' non lo prende nessuno."
     },
     "max": {
      "type": "integer",
      "minimum": 0,
      "maximum": 4,
      "description": "Il tetto per seggio, sommando titolo e maggioranza."
     },
     "commits_at_least": {
      "type": "integer",
      "minimum": 1,
      "maximum": 3,
      "description": "Quante carte bisogna aver impegnato perche' il peso conti."
     },
     "sides": {
      "type": "array",
      "items": {"type": "string", "enum": ["SUPPORT", "OPPOSE"]},
      "description": "Su quali fronti il peso si applica. Stare in un posto e' neutro rispetto al lato: «e' terra mia e dico di no» pesa quanto «e' terra mia e dico di si'»."
     },
     "includes_proponent": {
      "type": "boolean",
      "default": true,
      "description": "Se il proponente prenda il peso della terra. A true il bonus finisce quasi sempre a chi propone - che e' gia' scelto *per* la presenza - e i Consigli passano troppo: misurato, FAIL 164 e un seggio bloccato. A false il peso va solo a chi la terra ce l'ha ma il Consiglio non l'ha chiamato: «non si decide di casa mia senza di me»."
     }
    }
   },
   "alliance_weight": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "per_step": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 2,
                  "description": "Quanto pesa ogni passo di distanza da NEUTRAL, sul fronte su cui chi lo porta si schiera."
                },
                "max": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 3,
                  "description": "Il tetto per seggio: senza, due legami stretti deciderebbero il Consiglio da soli."
                },
                "commits_at_least": {
                  "type": "integer",
                  "minimum": 1,
                  "maximum": 3,
                  "description": "Quante carte l'alleato deve impegnare perche' la sua voce pesi. Un'alleanza che aiuta senza costare e' un bonus passivo; una che chiede di metterci del proprio e' una scelta al tavolo."
                }
              },
              "description": "Quanto pesa un legame al Consiglio (D-139). Un alleato che sostiene il proponente, e che impegna carte per farlo, parla piu' forte di uno sconosciuto: il bonus va al fronte SUPPORT e solo al legame caldo (ALLY un passo, BOUND due). La forma simmetrica - il nemico che osteggia pesa quanto l'alleato che sostiene - e' stata scritta, misurata e scartata: il tavolo di partenza ha ostilita' e non ha alleanze, quindi un dente simmetrico pesa da un lato solo (D-139). Chi tradisce il proprio legame non pesa di piu' ne' di meno: parla da solo. Omesso, i legami non toccano il tavolo - com'era prima di D-139."
            }
          }
        },
        "starting_structures": {
          "type": "array",
          "description": "Cosa c'e' gia' costruito quando l'anno si apre. Sta sulla Chronicle e non sulla Regione per la stessa ragione di `starting_control` (D-049): le Regioni sono condivise fra saghe distanti secoli, e una pietra scritta sulla Regione seduterebbe le case della prima saga al tavolo della seconda.",
          "items": {
            "type": "object",
            "required": ["region_id", "structure_type"],
            "additionalProperties": false,
            "properties": {
              "region_id": { "type": "string" },
              "structure_type": { "type": "string" },
              "grade": { "type": "integer", "minimum": 1, "maximum": 4, "default": 1 },
              "owner": { "type": ["string", "null"] }
            }
          }
        },
        "structure_rules": {
          "type": "object",
          "additionalProperties": false,
          "description": "La scala che si muove con l'esito del Destino (D-159). Omesso, le strutture non salgono e non cadono da sole. Il grado sale sulla struttura piu' alta di chi ha ottenuto quello che voleva, e scende su quella piu' bassa di chi non ha ottenuto niente: si costruisce sopra quello che si ha, e si perdono prima i margini.",
          "properties": {
            "rise_on": {
              "type": "array",
              "items": { "type": "string", "enum": ["MINIMUM", "VICTORY", "TRIUMPH"] },
              "description": "I livelli che fanno salire un grado."
            },
            "fall_on": {
              "type": "array",
              "items": { "type": "string", "enum": ["NONE", "MINIMUM", "VICTORY"] },
              "description": "I livelli che ne fanno perdere uno. Sotto il primo grado la struttura non scende: va in rovina, e la rovina lascia una cicatrice."
            }
          }
        },
        "control_rules": {
          "type": "object",
          "description": "How holding territory pushes back (D-027). Omit the whole object and control behaves as in v0.2: whatever you take, you keep.",
          "additionalProperties": false,
          "properties": {
            "contested": {
            "type": "object",
            "additionalProperties": false,
            "description": "La contesa del controllo (D-158). Omesso, il padrone di una Regione e' quello **scritto** da una Conseguenza e resta finche' un'altra non lo cambia. Dichiarato, il padrone e' **contato** a ogni fine round: chi somma di piu' fra il valore delle proprie strutture e le proprie pedine. A parita' non cambia niente — per togliere una Regione bisogna superare chi la tiene, non pareggiarlo.",
            "properties": {
              "presence_weight": {
                "type": "integer",
                "minimum": 0,
                "maximum": 5,
                "default": 1,
                "description": "Quanto vale una pedina nel conto, contro il valore di grado di una struttura. A 1 una torre di veglia (2) vale due pedine."
              }
            }
          },
          "max_stable_control": {
              "type": "integer",
              "minimum": 0,
              "description": "Regions an Entity can hold without strain. Every Region beyond this raises the Tension of that Region's own domain, once per round."
            },
            "overextension_delta": {
              "type": "integer",
              "minimum": 0,
              "default": 1,
              "description": "How much each Region held beyond the limit raises its domain's Tension."
            },
            "lapse_without_presence": {
              "type": "boolean",
              "default": false,
              "description": "At the start of a continuing Chronicle, a Region held with no presence token in it reverts to nobody. You cannot govern where you are not."
            }
          }
        },
        "tension_pool": {
          "type": "object",
          "description": "Assemble the Chronicle's Tensions from the library instead of listing them. The draw is seeded, so the same seed always deals the same year.",
          "additionalProperties": false,
          "required": [
            "candidates",
            "count"
          ],
          "properties": {
            "candidates": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "pattern": "^TEN_[A-Z0-9_]+$"
              },
              "description": "Tension ids the Chronicle may draw from."
            },
            "count": {
              "type": "integer",
              "minimum": 1,
              "description": "How many to draw."
            },
            "always": {
              "type": "array",
              "items": {
                "type": "string",
                "pattern": "^TEN_[A-Z0-9_]+$"
              },
              "description": "Drawn first, every time: the questions this Chronicle is about by definition."
            },
            "echoes": {
              "type": "object",
              "propertyNames": {
                "pattern": "^TEN_[A-Z0-9_]+$"
              },
              "additionalProperties": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "description": "I segni che richiamano una candidata: se il mondo ereditato porta uno di questi tag (globale, di Regione, o la sua legend:) la Tensione pesa il triplo nella pesca. E cosi che l'era dopo cresce da quella prima invece di essere pescata alla cieca (D-079)."
            }
          }
        },
        "enduring_facts": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "I fatti globali che restano fatti anche dopo un salto lungo, perche murati o scritti: tutto il resto diventa legend:<fatto> (D-075). Assente = niente resiste al tempo."
        },
        "era_tallies": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "id",
              "if_tag",
              "chain"
            ],
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^TLY_[A-Z0-9_]+$"
              },
              "if_tag": {
                "type": "string",
                "minLength": 1
              },
              "if_not_tag": {
                "type": "string"
              },
              "chain": {
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                },
                "minItems": 2
              }
            },
            "additionalProperties": false
          },
          "description": "Il conto delle ere nei segni (D-133): a ogni successione, se `if_tag` sta sui fatti globali dell'era chiusa (e `if_not_tag` no), il mondo posa il prossimo segno di `chain`; se la condizione cade, tutta la catena sparisce senza diventare leggenda - il conto riparte. E' cosi che tre ere col sigillo intatto diventano `mountain_forgotten`, il segno che fa sedere la Leggenda."
        },
        "years_after_previous": {
          "description": "Quanto tempo passa fra la Chronicle precedente e questa. Un numero, oppure un intervallo pescato dal seme: e cosi che una saga puo coprire secoli invece di anni consecutivi (D-045). Assente = 1 anno.",
          "oneOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 2000
            },
            {
              "type": "object",
              "required": [
                "min",
                "max"
              ],
              "additionalProperties": false,
              "properties": {
                "min": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 2000
                },
                "max": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 2000
                }
              }
            }
          ]
        },
        "destiny_pool": {
          "type": "object",
          "description": "Che cosa vuole ogni casa quest'anno (D-150). Per ogni entità, i Destini fra cui l'anno pesca — tutti scritti per quella casa: non si permuta niente fra le case, perché una clausola nomina Regioni, rivali e segni della sua epoca, e la prosa è scritta per chi la porta. Omesso, ogni casa insegue il `destiny_id` scritto sull'Entità, com'era prima di D-150. È il meccanismo di `tension_pool` applicato agli obiettivi, e serve a rompere il Minimo come risposta giusta di default: la sonda della varietà (D-149) dice che le storie sono già diverse (distanza 0,8) e che a ripetersi è cosa i giocatori vogliono.",
          "additionalProperties": {
            "type": "array",
            "minItems": 2,
            "items": {
              "type": "string",
              "pattern": "^DST_[A-Z0-9_]+$"
            },
            "description": "I Destini possibili per quella casa. Almeno due: uno solo sarebbe il campo che c'è già sull'Entità."
          }
        }
      }
    }
  }
}
