{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://echoes.local/schema/tension.schema.json",
  "title": "Tension collection",
  "x-echoes-def": "tension",
  "x-echoes-kind": "collection",
  "type": "object",
  "required": [
    "schema_id",
    "version",
    "items"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_id": {
      "const": "tension"
    },
    "version": {
      "type": "string"
    },
    "items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/tension"
      }
    }
  },
  "$defs": {
    "tension": {
      "type": "object",
      "required": [
        "id",
        "title",
        "domain",
        "current_value",
        "threshold",
        "visibility",
        "relevant_asset_families",
        "omen_thresholds",
        "possible_questions",
        "triggers",
        "decrease_rules",
        "linked_tensions",
        "theme"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^TEN_[A-Z0-9_]+$"
        },
        "title": {
          "type": "string",
          "minLength": 1
        },
        "domain": {
          "type": "string",
          "enum": [
            "TERRITORY",
            "RESOURCE",
            "SURVIVAL",
            "KNOWLEDGE",
            "ANCIENT"
          ]
        },
        "current_value": {
          "type": "integer",
          "minimum": 0
        },
        "threshold": {
          "type": "integer",
          "minimum": 1
        },
        "visibility": {
          "type": "string",
          "enum": [
            "OPEN",
            "VEILED",
            "SECRET"
          ]
        },
        "relevant_asset_families": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "enum": [
              "FORCE",
              "AUTHORITY",
              "PEOPLE",
              "KNOWLEDGE",
              "WEALTH",
              "BONDS"
            ]
          }
        },
        "omen_thresholds": {
          "type": "array",
          "description": "Public narrative signals fired when current_value first reaches `at`. Never reveals the number.",
          "items": {
            "type": "object",
            "required": [
              "at",
              "message"
            ],
            "additionalProperties": false,
            "properties": {
              "at": {
                "type": "integer",
                "minimum": 1
              },
              "message": {
                "type": "string",
                "minLength": 1
              },
              "reveals_value": {
                "type": "boolean",
                "default": false
              }
            }
          }
        },
        "possible_questions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Question ids from the Confluence template that this Tension may raise."
        },
        "physical": {
          "type": "object",
          "description": "La faccia fisica della carta Tensione (D-280): le due liste su cui si posano le pedine, e cosa succede se la proposta cade. Il proponente compra i benefici; gli avversari scelgono in che moneta paga — un costo per ogni beneficio oltre il primo, fino a un tetto di tre (D-303: la Cicatrice e' un costo come gli altri, non una moneta d'acquisto).",
          "additionalProperties": false,
          "properties": {
            "benefits": {
              "type": "array",
              "minItems": 2,
              "description": "Cosa la proposta puo' ottenere sul tavolo. Il proponente ne posa fino a tre (quattro accettando una Cicatrice).",
              "items": {
                "type": "object",
                "required": [
                  "id",
                  "verb",
                  "text"
                ],
                "additionalProperties": false,
                "properties": {
                  "id": {
                    "type": "string",
                    "pattern": "^[A-Z0-9_]+$",
                    "description": "Id della voce, unico dentro la carta."
                  },
                  "verb": {
                    "type": "string",
                    "enum": [
                      "REOPEN",
                      "CLEAR_CONDITION",
                      "BUILD_STONE",
                      "TAKE_CONTROL",
                      "COOL_THEME",
                      "REMEMBER",
                      "COOL_QUESTION"
                    ],
                    "description": "Il verbo del vocabolario chiuso (D-280): e' il motore a eseguirlo. Agisce sul luogo di cui si discute, tranne IL MONDO RICORDA (D-308), che posa una memoria sul mondo intero: e' il verbo con cui il Consiglio decide cosa il mondo ricordera'."
                  },
                  "text": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Come si legge sulla carta, sotto l'icona del verbo."
                  },
                  "tag": {
                    "type": "string",
                    "description": "Il segno che il verbo posa o toglie, dove il verbo lo chiede (AGGIUNGI CONDIZIONE, CICATRICE, IL MONDO RICORDA; opzionale per RIMUOVI CONDIZIONE)."
                  },
                  "structure": {
                    "type": "string",
                    "pattern": "^STR_[A-Z0-9_]+$",
                    "description": "La Pietra che COSTRUISCI PIETRA alza."
                  }
                }
              }
            },
            "costs": {
              "type": "array",
              "minItems": 2,
              "description": "Il prezzo che la proposta lascia sulla mappa. Lo scelgono gli avversari, uno per ogni beneficio oltre il primo (massimo due).",
              "items": {
                "type": "object",
                "required": [
                  "id",
                  "verb",
                  "text"
                ],
                "additionalProperties": false,
                "properties": {
                  "id": {
                    "type": "string",
                    "pattern": "^[A-Z0-9_]+$",
                    "description": "Id della voce, unico dentro la carta."
                  },
                  "verb": {
                    "type": "string",
                    "enum": [
                      "ADD_CONDITION",
                      "TOLL",
                      "YIELD_CONTROL",
                      "HEAT_THEME",
                      "TAKE_DEBT",
                      "SCAR",
                      "HEAT_QUESTION"
                    ],
                    "description": "Il verbo del vocabolario chiuso (D-280): e' il motore a eseguirlo. Agisce sul luogo di cui si discute."
                  },
                  "text": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Come si legge sulla carta, sotto l'icona del verbo."
                  },
                  "tag": {
                    "type": "string",
                    "description": "Il segno che il verbo posa o toglie, dove il verbo lo chiede (AGGIUNGI CONDIZIONE, CICATRICE, IL MONDO RICORDA; opzionale per RIMUOVI CONDIZIONE)."
                  },
                  "structure": {
                    "type": "string",
                    "pattern": "^STR_[A-Z0-9_]+$",
                    "description": "La Pietra che COSTRUISCI PIETRA alza."
                  }
                }
              }
            },
            "failure": {
              "type": "array",
              "minItems": 1,
              "description": "Se la proposta non passa: il mondo non sopporta l'indecisione, e questi effetti si applicano comunque. Non si scelgono — sono stampati.",
              "items": {
                "type": "object",
                "required": [
                  "id",
                  "verb",
                  "text"
                ],
                "additionalProperties": false,
                "properties": {
                  "id": {
                    "type": "string",
                    "pattern": "^[A-Z0-9_]+$",
                    "description": "Id della voce, unico dentro la carta."
                  },
                  "verb": {
                    "type": "string",
                    "enum": [
                      "ADD_CONDITION",
                      "TOLL",
                      "YIELD_CONTROL",
                      "HEAT_THEME",
                      "TAKE_DEBT",
                      "SCAR"
                    ],
                    "description": "Il verbo del vocabolario chiuso (D-280): e' il motore a eseguirlo. Scatta da solo quando la proposta cade."
                  },
                  "text": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Come si legge sulla carta, sotto l'icona del verbo."
                  },
                  "tag": {
                    "type": "string",
                    "description": "Il segno che il verbo posa o toglie, dove il verbo lo chiede (AGGIUNGI CONDIZIONE, CICATRICE, IL MONDO RICORDA; opzionale per RIMUOVI CONDIZIONE)."
                  },
                  "structure": {
                    "type": "string",
                    "pattern": "^STR_[A-Z0-9_]+$",
                    "description": "La Pietra che COSTRUISCI PIETRA alza."
                  }
                }
              }
            }
          }
        },
        "triggers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Human-readable notes on what pushes this Tension up."
        },
        "decrease_rules": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Human-readable notes on what pulls this Tension down."
        },
        "linked_tensions": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^TEN_[A-Z0-9_]+$"
          }
        },
        "confluence_template_id": {
          "type": "string",
          "pattern": "^CNF_[A-Z0-9_]+$"
        },
        "description": {
          "type": "string"
        },
        "focus_region_tags": {
          "type": "array",
          "description": "Region tags, most specific first, naming what this Tension is about. The engine picks the Region in the Tension's domain carrying the earliest matching tag and binds it to $region in the authored text, so one question can follow the crisis around the map instead of naming a Region for ever.",
          "items": {
            "type": "string"
          }
        },
        "opening_line": {
          "type": "string",
          "minLength": 1,
          "description": "The sentence the world says about this question when it is on the table at a Chronicle's opening. A Chronicle that draws its questions composes its opening from these instead of writing one paragraph that would name questions it might not have drawn (D-207)."
        },
        "theme": {
          "type": "string",
          "pattern": "^THM_[A-Z_]+$",
          "description": "Il Tema fisico a cui questa Tensione appartiene sul tavolo. `domain` resta la parola del motore; `theme` e' la traccia di Calore che i giocatori guardano, e le due non coincidono: la Successione e' TERRITORY per il motore e Potere per chi gioca."
        },
        "council": {
          "type": "object",
          "description": "**Il Consiglio che questa carta apre** (0.1.272, taglio 2 di ISSUES 80, parola del committente: «ogni carta sue proposte»). Le Domande e le Proposte stanno sulla carta, non in un template condiviso: al tavolo la carta girata deve bastare a se stessa. Il template resta per quello che non e' della singola carta — le clausole, i pool, la Risonanza del titolo.",
          "required": [
            "questions",
            "propositions"
          ],
          "additionalProperties": false,
          "properties": {
            "questions": {
              "type": "array",
              "minItems": 1,
              "items": {
                "$ref": "confluence_template.schema.json#/$defs/question"
              }
            },
            "propositions": {
              "type": "array",
              "minItems": 2,
              "items": {
                "$ref": "confluence_template.schema.json#/$defs/proposition"
              }
            }
          }
        },
        "heats_when": {
          "type": "array",
          "description": "**SI ACCENDE QUANDO** — la casella che il committente ha disegnato sulla faccia della Tensione. Dice, in regola leggibile al tavolo, cosa le fa prendere Calore: non e' prosa, e' quello che l'Azione appena giocata ha **fatto al mondo**. Fino alla 0.1.292 il Calore andava alla questione piu' vicina alla soglia del Tema — un ponte che il codice stesso dichiarava provvisorio (D-261). Con queste righe il Calore va alla questione che **quel gesto** riguarda. Vuota o assente: vale il ponte, ed e' il ripiego dichiarato.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "text"
            ],
            "properties": {
              "text": {
                "type": "string",
                "description": "La riga come si stampa sulla carta: «aggiunge #conteso su una Regione con #campo o #granaio»."
              },
              "puts_tag": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "L'Azione ha posato uno di questi segni."
              },
              "clears_tag": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "L'Azione ha tolto uno di questi segni."
              },
              "builds": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "L'Azione ha costruito una di queste Pietre (id di structure_type)."
              },
              "takes_control": {
                "type": "boolean",
                "description": "L'Azione ha cambiato il controllo di una Regione."
              },
              "removes_presence": {
                "type": "boolean",
                "description": "L'Azione ha tolto una Presenza da una Regione."
              },
              "adds_presence": {
                "type": "boolean",
                "description": "L'Azione ha portato una Presenza in una Regione. E' il gesto piu' frequente che le Azioni fanno alla mappa — 175 volte su vent'anni — e fino alla 0.1.298 nessun verbo lo nominava, quindi non poteva accendere niente (D-335)."
              },
              "on_region_with": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Filtro: la Regione toccata deve portare uno di questi segni. Senza, vale ovunque."
              }
            }
          }
        }
      }
    }
  }
}
